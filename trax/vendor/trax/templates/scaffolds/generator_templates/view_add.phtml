<h1>New <?= $singluar_model_name ?></h1>

< ?= start_form_tag(array(":action" => "add")) ? >
    < ?= $this->render_partial("form") ? ><br>
    < ?= submit_tag("Create") ? >
< ?= end_form_tag() ? >

<br>

< ?= link_to("Back", array(":action" => "index")) ? >
